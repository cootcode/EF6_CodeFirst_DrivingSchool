@model IEnumerable<EF6_CodeFirst_DrivingSchool.Models.Ref_Lesson_Status>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LessonStatusDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LessonStatusDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LessonStatusCode }) |
            @Html.ActionLink("Details", "Details", new { id=item.LessonStatusCode }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LessonStatusCode })
        </td>
    </tr>
}

</table>
